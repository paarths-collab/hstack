# local state / secrets — never commit
*.env
.env
.env.*
*.key
secrets/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# scratch
tmp/
scratch/

# dashboard: the app code IS tracked; dev artifacts (screenshots, uploads) are not
dashboard/scraps/
dashboard/uploads/
**/.thumbnail

# node / next build output (never commit these)
node_modules/
.next/
